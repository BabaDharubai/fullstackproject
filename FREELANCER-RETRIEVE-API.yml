db.user: baba
db.password: baba
db.portNo: 5985
db.hostName: database-couch
db.name: freelancer

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/freelancer
